services:
  pcloud-mcp:
    build: .
    ports:
      - "3847:3847"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      PORT: "3847"
    volumes:
      - pcloud-mcp-data:/data
    restart: unless-stopped

volumes:
  pcloud-mcp-data:
